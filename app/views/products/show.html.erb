<p id="notice"><%= notice %></p>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>

<% if current_user && user_signed_in? %>
<%= link_to 'Add to cart',  product_cart_products_path(@product.id), method: 'post', remote: true %>
<% end %>

<%if @product.cat_picts.attached?%>

<%= image_tag @product.cat_picts[0], data: { toggle:"modal", target:".bd-example-modal-lg"}, class:'add_watermark' %>

<% end %>